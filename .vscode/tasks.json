{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "0.1.0",
    "tasks": [
        {
            "taskName": "build",
            // "command": "c++",
            "command": "make",
            "isShellCommand": true,
            "args": ["-I${workspaceRoot}/include"],
            // "args": ["-g", "-Wall" ,"-std=gnu++11", "${file}", "-o", "bin/${fileBasenameNoExtension}"],"
            "showOutput": "always"
        },
        {
            "taskName": "run",
            "command": "./bin/${fileBasenameNoExtension}", 
            "args": [],
            "isShellCommand": true,
            "showOutput": "always"
        }
    ]
}